真是不走运，第一次写的东西直接就崩溃了，这一次些少点把。


1，代码风格一定要统一，特别是小组人比较多的情况下，统一的代码风格将会让我们的修改bug的速度大增。
2，重构是一柄双刃剑，两面都很锋利啊。
3，场景的重现到底对修改bug意味这什么？
在莫种场景下能够复现这个bug，那么对照到程序上，我们可以对照到该种场景的逻辑，很多东西将变的明确，至少知道针对好几页的代码指导针对其中的那一部分了。

4，service的启动onbindService和StartService是可以混用的，onStartCommand和onBind都是可以被调用n次的，在什么时候结束该service？当该service不会被任意activity持有，并且调用自己的onDestroy方法后，那么他将被销毁。

5，判空，这是一个在程序中又臭又长的裹脚布，在遇到这样的情况，我们应该在所有的get方法上都设置get
